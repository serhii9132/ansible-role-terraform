---
- name: Converge
  hosts: all
  gather_facts: true
  tasks:
    - name: "Include ansible-role-terraform"
      ansible.builtin.include_role:
        name: "../../../ansible-role-terraform/" # noqa: role-name
